# Copy to /srv/jenkins/controller.env on the Jenkins host. Keep that real file
# local to the host; it must never be committed.
JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=replace-with-a-long-unique-password
JENKINS_URL=http://replace-with-controller-public-ip:8080/
